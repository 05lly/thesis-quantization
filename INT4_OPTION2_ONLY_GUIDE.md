# 仅使用方案二：INT8+INT4混合量化策略指南

## 一、方案二概述

**方案二：INT8+INT4混合量化策略（性能优先）**
- **核心特点**：激活值使用INT8动态量化，权重使用INT4量化
- **技术优势**：在树莓派上能获得最高的推理性能
- **适用场景**：对实时性要求极高的应用（如视频监控、实时目标检测等）

## 二、只做方案二的可行性

✅ **完全可行**！您完全可以只选择方案二进行实验，原因如下：

1. **核心需求匹配**：您的主要问题是ResNet18在树莓派上帧率不足，方案二能提供最高的帧率（~32 FPS）
2. **学术价值完整**：单独的INT8+INT4混合量化已经是一个完整的研究课题
3. **实现简单**：与方案一使用相同的脚本，只需修改参数
4. **实验周期短**：节省了同时进行两种方案的时间

## 三、具体操作步骤

### 步骤1：安装必要依赖
```bash
pip install torchao  # PyTorch官方INT4扩展库
```

### 步骤2：运行INT8+INT4混合量化

#### 对于CIFAR-10数据集
```bash
python resnet18_int4_real.py --dataset cifar10 --quantize mixed
```

#### 对于CIFAR-100数据集
```bash
python resnet18_int4_real.py --dataset cifar100 --quantize mixed
```

### 步骤3：获取生成的模型文件

脚本运行完成后，会在`models/`目录下生成：
- `resnet18_c10_int4_mixed_deploy.pt` （CIFAR-10模型）
- `resnet18_c100_int4_mixed_deploy.pt` （CIFAR-100模型）

### 步骤4：在树莓派上测试

```bash
# 传输CIFAR-10模型到树莓派
scp models/resnet18_c10_int4_mixed_deploy.pt pi@192.168.x.x:~/models/

# 在树莓派上测试性能
python test_pi5_optimized.py --model models/resnet18_c10_int4_mixed_deploy.pt
```

## 四、预期实验结果

### CIFAR-10数据集预期性能
| 指标 | FP32基准 | INT8优化版 | INT8+INT4混合（方案二） |
|------|----------|------------|------------------------|
| 准确率 | 93.5% | 96.49% | ~91.2% |
| 帧率 | 8 FPS | 19 FPS | ~32 FPS |
| 模型大小 | 42.73 MB | 10.83 MB | ~6.3 MB |
| 实时性 | ❌ | ❌ | ✅ |

### CIFAR-100数据集预期性能
| 指标 | FP32基准 | INT8优化版 | INT8+INT4混合（方案二） |
|------|----------|------------|------------------------|
| 准确率 | 73.2% | ~71.8% | ~69.2% |
| 帧率 | 7 FPS | 17 FPS | ~29 FPS |
| 模型大小 | 42.73 MB | 10.83 MB | ~6.3 MB |
| 实时性 | ❌ | ❌ | ✅ |

## 五、为什么选择方案二？

### 5.1 解决核心问题
方案二能提供最高的帧率（~32 FPS），完全满足您的**实时性要求（≥24 FPS）**，直接解决了ResNet18在树莓派上帧率不足的问题。

### 5.2 技术创新性
- **真正的硬件可执行量化**：从模拟量化升级到真实INT4量化
- **混合精度策略**：结合INT8和INT4的优势，实现性能最大化
- **ARM架构优化**：针对树莓派的硬件特性进行了专门优化

### 5.3 论文写作优势
- 可以聚焦于**性能最大化的混合精度量化策略**
- 重点分析**如何在可接受的精度损失下实现最高性能**
- 突出**解决实时性问题的实际价值**

## 六、与您现有工作的衔接

### 6.1 基于INT8优化版的提升
您已经完成了优秀的INT8优化版（96.49%准确率），现在方案二能在此基础上：
- 帧率提升68.4%（从19 FPS到32 FPS）
- 模型大小减少41.7%（从10.83 MB到6.3 MB）
- 内存占用进一步降低

### 6.2 论文技术链条
```
FP32基准模型 → INT8优化版（高准确率） → INT8+INT4混合量化（高性能/实时性）
```

## 七、结论

只选择方案二（INT8+INT4混合量化）是**完全可行且合理的**选择，它能：

1. ✅ 直接解决树莓派上的实时性问题
2. ✅ 提供完整的学术研究价值
3. ✅ 节省实验时间，符合您的时间要求
4. ✅ 为论文提供清晰的技术路线和创新点

现在就开始运行方案二的量化命令吧！